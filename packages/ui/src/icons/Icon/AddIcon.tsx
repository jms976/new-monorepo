import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Add from '../svg/Add.svg';

const AddIcon = (props: IconProps) => <CreateIcon Icon={Add} viewBox="0 0 17 16" {...props} />;
export default AddIcon;
