import { type IconProps } from '../types';
import CreateIcon from '../CreaateIcon';

import Alarm from '../svg/Alarm.svg';

const AlarmIcon = (props: IconProps) => <CreateIcon Icon={Alarm} viewBox="0 0 22 28" {...props} />;
export default AlarmIcon;
